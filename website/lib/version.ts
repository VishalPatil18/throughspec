// Single source of truth for the published CLI version shown across the site.
// Bump this on release; it mirrors packages/*/version and CHANGELOG.md's latest tag.
export const SITE_VERSION = '1.2.0';
