#!/usr/bin/env node
// Throughspec - spec-init (Node CLI)
// Stage 1 stub: proves the build and distribution pipeline.
// Real command surface lands in Stage 3.

const VERSION = '0.0.0';

function main(): void {
  console.log(`Throughspec spec-init v${VERSION}`);
  console.log('Stage 1 stub - real CLI lands in Stage 3.');
}

main();
